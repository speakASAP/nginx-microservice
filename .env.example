# Nginx Microservice Environment Variables

# Let's Encrypt Configuration
CERTBOT_EMAIL=
CERTBOT_STAGING=

# Docker Network Configuration
NETWORK_NAME=

# Note: Ports (80 for HTTP, 443 for HTTPS) are configured in docker-compose.yml
# and are not configurable via environment variables to ensure consistency
# across the microservices ecosystem.

# Docker Volumes Base Path
DOCKER_VOLUMES_BASE_PATH=

# Production Paths
PRODUCTION_BASE_PATH=
DATABASE_SERVER_PATH=

# Docker Images
HEALTH_CHECK_IMAGE=
CERTBOT_IMAGE=
POSTGRES_IMAGE=
REDIS_IMAGE=
